spring:
  liquibase:
    change-log: db/changelog/changelog-master.yaml
  jpa:
    show-sql: true
  grpc:
    server:
      port: 0
    client:
      default-channel:
        address: 0.0.0.0:\${local.grpc.port}